# LSP Fact Consumption Ledger

This ledger records what `marrow-lsp` may treat as semantic authority while
Marrow core is moving to v0.1. It is the surface-status authority for the
release contract in
[`production-readiness-roadmap.md`](production-readiness-roadmap.md). The rule is
direct: if a fact belongs to Marrow and Marrow does not expose it, LSP, MCP, DAP,
and VS Code must delete the surface, block it, or render an explicitly
presentation-only view. They must not reconstruct language, catalog, runtime, or
storage meaning locally.

## Status Definitions

| Status | Meaning |
| --- | --- |
| `ready` | The surface consumes current public Marrow facts and does not decide language, catalog, storage, or runtime semantics locally. |
| `presentation-only` | The surface is UI over source text, diagnostics, transport output, packaging, or bounded debug/admin data. It is not a stable semantic contract. If users can invoke it, the contract must be explicit, bounded, tested, and impossible to confuse with production semantics. |
| `blocked-on-marrow` | The surface needs a Marrow-owned fact that is not exposed, and the executable feature path is absent or returns a stable unavailable result. |
| `deleted` | The prior implementation depended on removed or private Marrow details and has no active runtime/editor path. |

## Current Marrow API Rebase Notes

Several old blockers predate current Marrow v0.1 APIs. These APIs do not make
the current LSP, MCP, DAP, or VS Code surfaces `ready` by themselves; they mark
the next work as LSP adoption unless a lane proves a narrower upstream gap:

- Runtime/session adoption candidates:
  `marrow_run::ProjectSession`, `ProjectOpen`, `ProjectMode`,
  `SessionEntry`, `CheckedEntryCall`, `run_entry`, `run_entry_with_debugger`,
  and read-only expression evaluation.
- Checked-program adoption candidates:
  source digests, evolution digests, read-only context digests,
  entry footprints, entry cost shapes, entry store-open modes, runtime
  programs, and checked read-only expressions.
- Data tooling adoption candidates:
  saved-data path resolution, source-text path resolution, read, render,
  child-page, walk, root-listing, count, and integrity sampling or visiting
  APIs.
- Store/admin adoption candidates:
  `CommitMetadata`, store UID, catalog snapshot digest, read-only snapshots,
  and catalog snapshot reads.

Rows below that mention these APIs are current implementation limits, not
proof that Marrow lacks the fact.

## LSP Adoption Candidates

These surfaces are safe in their current narrowed form, but must no longer be
treated as upstream API requests until an LSP lane proves a more specific gap.

| Surface | Current safety contract | Marrow API candidates | Next LSP lane |
| --- | --- | --- | --- |
| DAP launch entry invocation | Configured and explicit-entry launch uses isolated Marrow project sessions and remains a narrowed debug helper | `SessionEntry`, canonical entry DTOs, typed launch-argument DTOs, debugger facts | Entry selectors, typed args, plain-line stop points, stopped-frame source references, local runtime value expansion, and stopped-frame durable data inspection are admitted through Marrow facts. Production graduation still needs store-generation and serve/attach facts. |
| Saved Resource Inspector children and values | Stable Marrow saved-data DTOs, typed child expansion, bounded child pages, Marrow-owned bounded read presence, bounded value previews, Marrow data-view boundary facts, configured native dev-store plus committed-lock refresh behind `marrow.liveData`, and Marrow-owned surface-session admission for fresh checked programs | Shared saved-data path resolution, child, read, walk, render, cursor, preview APIs, `ProjectSurfaceReadSession::data_view_boundary`, `data_view_watch_targets`, and `ProjectSurfaceReadSession::admits_checked_program` | Root listing, child pages, and value reads carry Marrow `data_view_boundary` facts with source analysis generation, store snapshot, admitted compatibility, and Marrow-derived watch targets. `store_snapshot` remains as generation/snapshot metadata retained for current clients. Saved-data session reuse is admitted by Marrow's surface-session predicate, including the lock-bound checked-program case for the same source and accepted native store. Memory projects and disabled live data return no watch targets through downstream gating. Production live-data use still needs integrity, repair, serve, attach, and broader production validation contracts. |
| MCP data roots and children | Stable Marrow saved-data DTOs, data-view boundary DTOs, and typed saved-data path protocol through the shared core | Shared saved-data path resolution, root-listing, child, walk, render, cursor APIs, and `ProjectSurfaceReadSession::data_view_boundary` | Root listing and child pages carry Marrow `data_view_boundary` facts plus generation/snapshot metadata retained for current clients. Production serving still needs integrity, repair, serve, and attach contracts. |
| MCP surface operations | Stable Marrow surface ABI, route manifest, operation request/response/error DTOs, and read-only/writable project surface sessions | `marrow_json::surface::{SurfaceAbiJson, SurfaceRouteManifestJson, SurfaceOperationRequestJson, execute_project_surface_operation_read_only, execute_project_surface_operation}` and `marrow_run::{ProjectSurfaceReadSession, ProjectSurfaceSession}` | `mw_surface_routes` exposes read routes by default and read/update/action routes only when explicitly requested, without store access. `mw_surface_read` is gated by MCP data access and delegates read execution and write/action rejection to Marrow's canonical read-only executor. `mw_surface_write` is gated by MCP data access and delegates read/update/action execution to Marrow's canonical writable executor. Raw `mw_data_*` tools remain presentation-only. |
| Data integrity samples | Advisory capped scan with Marrow integrity sample DTOs, data-view boundary facts, and generation/snapshot metadata retained for current clients | `marrow_json::saved_data::DataIntegrityResultJson`, integrity sampling and visiting APIs, store metadata APIs, and `ProjectSurfaceReadSession::data_view_boundary` | Keep repair and production-validation claims blocked until Marrow exposes drift witnesses, typed repair facts, and production validation contracts. |

## Current Surface Matrix

| Surface | Status | Current authority | Missing Marrow facts / rule |
| --- | --- | --- | --- |
| LSP initialize/capability advertisement | `ready` | Static capability map in the LSP transport | Capabilities must not imply production semantic contracts beyond the rows below. |
| LSP text sync and diagnostics publish | `ready` | Full-document sync, Marrow diagnostics, spans, severity, and diagnostic codes | LSP renders Marrow diagnostics only. Logic and tests must not infer meaning from diagnostic prose. |
| Workspace analysis cache | `presentation-only` | `marrow_project`, `analyze_project`, `AnalysisSnapshot::generation()`, `AnalysisGeneration`, generation digests, committed catalog lock watcher input, `CheckedProgram`, `build_binding_index` | Workspace snapshot and binding-index reuse consume Marrow analysis generation, including config, checked-source, read-only context, and catalog generation digests. File watching invalidates analysis for source/test files and the committed catalog lock input. Production stability still needs store/runtime generation facts and the broader source-intelligence surfaces below to consume checker-owned facts. |
| Diagnostics | `ready` | Marrow diagnostics, spans, severity, and diagnostic codes | LSP renders Marrow diagnostics only. Logic and tests must not infer meaning from diagnostic prose. |
| Formatting | `ready` | `marrow_syntax::format_source` and parse-error refusal | No local formatter semantics. |
| Hover: locals, params, functions, resources, enums, builtins, modules, roots | `presentation-only` | `BindingIndex`, `marrow_check::tooling::source_symbol_docs_at` for source-symbol docs, `marrow_check::tooling::source_type_hover_fact_at` for plain type hover fallbacks, `marrow_check::tooling::source_callable_hover_fact_at` for user function, parameter-use, module const, and intrinsic callable leaf hovers, `marrow_check::tooling::source_operator_hover_fact_at` for language-operator hovers, `marrow_check::tooling::source_module_path_hover_fact_at` for standard-library namespace/module and project module path hovers, `marrow_check::tooling::source_schema_hover_fact_at` for resource, enum, and enum-member declaration/reference hovers plus enum type-annotation leaf hovers, `marrow_check::tooling::saved_place_hover_fact_at` for saved member/index hovers, `marrow_check::tooling::store_root_hover_fact_at` for saved-root declaration hover, and direct effect facts for rendering callable effects | Resource declaration-name, constructor leaf, and type-annotation leaf hovers consume Marrow source schema hover facts. Enum declaration-name, type-annotation leaf, and member declaration/reference leaf hovers consume the same Marrow fact API, including catalog-backed enum annotation visibility and refusal of module/path qualifier prefixes. Source-symbol doc attachment consumes Marrow docs facts instead of walking declarations in LSP and is target-aware for source functions through Marrow source callable hover facts. Plain type hovers, including documented module-constant uses, consume Marrow source type hover facts rather than combining cursor type and docs in the LSP. Function, parameter-use, module const, and intrinsic callable leaf hovers for builtins, scalar conversions, `Error`, `Id`, and standard-library operations consume Marrow source callable hover facts with file-context import expansion; LSP renders Markdown only. Language-operator hovers consume Marrow source operator hover facts, including broken-buffer refusal for keyword path/declaration positions. Standard-library namespace/module and project module path hovers consume Marrow source module-path hover facts; LSP renders Markdown only for those surfaces. Saved field, layer, store-index, and saved-root declaration hovers consume Marrow hover facts for cursor gating and display data. The broader surface still needs production contracts for transitive effects and versioned live-data binding before it can be a production semantic contract. |
| Go-to definition, references, prepare rename, rename edits | `presentation-only` | `BindingIndex`, `SymbolRef`, `SymbolKind`, `RenameSafety`, `BindingIndex::rename_action`, `BindingIndex::source_only_rename_occurrence_at`, `BindingIndex::source_only_rename_action_at`, `marrow_check::tooling::source_module_path_definition_fact_at`, `marrow_check::tooling::source_saved_root_cursor_fact_at`, `marrow_check::tooling::source_catalog_definition_fact_at`, `marrow_check::tooling::source_catalog_reference_facts_at`; parsed source for outline only | Saved roots, resource members, store indexes, enum type-annotation leaves, and enum member literals consume Marrow source catalog navigation facts for definition/reference target selection, canonical catalog spans, and enum type-annotation filtering. Imported module aliases, qualified function/resource/enum leaves, and evolve transform body resource type-annotation leaves consume BindingIndex facts where Marrow emits them. Definition and reference locations render Marrow `SymbolRef` or source catalog navigation spans directly instead of recovering names from source text. Exact project module-prefix definition gaps consume Marrow source module-path definition facts after BindingIndex declines the cursor; namespace-only prefixes and keyword-shaped aliases stay unavailable instead of being tokenized locally. Saved-root syntax-only fallback suppression consumes Marrow source saved-root cursor facts after catalog facts decline the cursor, including unresolved roots in broken buffers, so the LSP no longer tokenizes root spelling itself. Source-only `prepareRename` consumes Marrow source-only rename occurrence spans. Source-only rename consumes Marrow source-only rename actions, including replacement-identifier validation and typed refusal reasons, rather than lexing replacement text or reconstructing cursor windows in the LSP. Imported module aliases have no v0.1 alias syntax and are refused when Marrow returns no action. The broader surface remains presentation-only because references, rename, remaining type-annotation hover/rename coverage, and durable identity facts for complete resource/store identity are not all production-semantic yet. Source spelling is not durable identity. Saved-data-backed edits still refuse because no canonical editor application target exists for evolve intent. |
| Document and workspace symbols | `ready` | Document symbols consume `marrow_check::tooling::document_symbols` for parsed-source outline facts. Workspace symbols consume `marrow_check::tooling::source_symbols_matching` for checked functions/constants, catalog-backed resource/store/index/member/enum/member facts, and Marrow-owned matching/filtering/ranking. | Document outline kind, detail, full range, selection range, and child nesting are Marrow-backed while preserving broken-buffer parse behavior. Workspace symbol kind, display name, source file/span, container ownership, and search ordering are Marrow-backed. LSP owns only protocol translation; sibling hover and navigation gaps remain tracked in their own rows. |
| Semantic tokens | `ready` | `marrow_check::tooling::source_semantic_token_facts` and `source_semantic_token_facts_for_file` | Marrow owns syntax baseline roles, saved-root sigil/name roles, declaration roles, context-free callable roles, snapshot-bound binding-index reference roles, snapshot-bound callable default-library roles, module-const readonly roles, and checked `Id(^root)` identity type-constructor roles as typed source facts. Stale buffers use the syntax/parse-only API; checked roles use only the snapshot-bound file API after the LSP freshness check. LSP owns only role/modifier-to-legend mapping and delta encoding. Architecture tests forbid local semantic-token classifier modules and `SymbolKind`/`TokenKind` role ownership in the semantic-token mapper. |
| Completion | `ready` | `marrow_check::tooling::source_completion_fact` owns the `source.completion.v1` protocol-free source completion item list with semantic item kinds, labels, detail text, docs, and a stable profile version. It composes Marrow cursor context, lexical saved-path receiver recovery through `source_saved_path_completion_fact_at`, checked saved-path context facts, saved-root facts, saved-path declared-member facts, namespace facts, type-position facts, typed in-scope value and module-alias facts, current-module resource/function/enum facts, raw standard-library namespace facts, expected enum-member value facts for annotated local const/var initializers, enum return expressions, typed callable parameters including resource constructors, assignment values, relative match arms, and intrinsic callable facts. | LSP owns only `CompletionItem` protocol mapping and Markdown wrapping. Local protocol code must not infer language candidates from source text when a Marrow completion fact exists. |
| Signature help | `ready` | `marrow_check::tooling::source_signature_help_fact_at` | Source call shape, active argument, named argument, callable resolution, documentation, parameter semantics, resource constructors, builtins, scalar conversions, `Error`, `Id`, and standard-library signatures consume Marrow signature-help facts with file-context import expansion. LSP owns only lexical/protocol translation, Markdown wrapping, and `SignatureHelp` rendering. |
| Custom LSP `marrow/savedRoots` | `presentation-only` | Typed saved-root views with catalog-bound `store_catalog_id` root segments plus Marrow `data_view_boundary` facts when `marrow.liveData` is enabled and the opened saved-data session is admitted by `ProjectSurfaceReadSession::admits_checked_program` against the fresh editor-visible checked program | Kept as the root listing entry point for the Saved Resource Inspector. TypeScript consumes the returned root segment and boundary object instead of deriving child paths or compatibility from display text or `store_snapshot`. Production use still needs integrity, repair, serve, attach, and broader production validation contracts. |
| Custom LSP `marrow/dataChildren` and `marrow/dataRead` | `presentation-only` | Stable Marrow saved-data DTOs, catalog-bound typed path segments (`store_catalog_id` for roots and `member_catalog_id` for fields/layers), bounded child pages, Marrow-rendered labels, Marrow-owned bounded read presence, Marrow bounded value previews, and Marrow `data_view_boundary` facts over the committed native dev store; invalid empty paths, zero limits, and old display-string segment authority fail before project or store reads; sessions are refused through Marrow surface-session admission after an editor recompute when the saved-data session no longer matches the fresh checked program | Refresh for this narrowed inspector is handled only by `marrow/dataWatchTargets` over Marrow-derived watch facts for the configured native dev-store file and committed catalog lock. Production live saved-data inspection still needs integrity, repair, serve, attach, and broader production validation contracts. |
| Custom LSP `marrow/dataWatchTargets` | `presentation-only` | `marrow_run::data_view_watch_targets` | Returns absolute filesystem paths for Marrow-derived watch targets only when `marrow.liveData` is enabled. It opens no store, derives no path in TypeScript, returns no store-file target for memory projects, and does not claim serve, attach, debuggee, repair, integrity, or production validation watching. |
| Custom LSP `marrow/dataIntegrity` | `presentation-only` | Advisory debug/admin command output using `marrow_json::saved_data::DataIntegrityResultJson`, Marrow `data_view_boundary` facts, and generation/snapshot metadata retained for current clients only where backed by current public tooling and admitted against the fresh editor-visible checked source/read-only context | Production validation still needs drift witnesses, typed repair facts, and production validation contracts. |
| Live saved-data hover and live record counts | `deleted` | None | Removed until LSP carries source/store generation boundaries for saved-data reads and counts. |
| LSP stdio transport | `ready` | Transport over `marrow-lsp-core` | Inherits the core feature statuses above. |
| MCP initialize/tools transport | `ready` | Thin stdio adapter over `marrow-lsp-core` | Individual tools carry their own status. |
| MCP request `params` envelope | `ready` | MCP stdio transport validation of supported request `params` containers | Supported methods accept absent, `null`, and object params, and reject concrete non-object params with `INVALID_PARAMS`. Unknown methods stay `METHOD_NOT_FOUND`; notifications without `id` draw no reply. No language semantics. |
| MCP `mw_check` | `ready` | Marrow project/snippet diagnostics and diagnostic codes | No local checker semantics. |
| MCP `mw_type_at` | `ready` | `marrow_check::type_at` | No local type inference. |
| MCP `mw_complete` | `ready` | Bounded transport envelope over `marrow_check::tooling::source_completion_fact`; MCP returns the `source.completion.v1` profile version, maps Marrow semantic item kinds to stable lowercase JSON strings, and does not pass through LSP `CompletionItem` fields. | Production source completion API. The MCP layer owns only argument decoding, JSON rendering, and the ready contract envelope. |
| MCP `mw_namespace_complete` | `ready` | `marrow_check::tooling::source_namespace_completion_file_fact` | Narrow production source namespace completion fact surface for explicit module/enum qualifiers only. Inputs are an absolute project file and non-empty namespace/name segments; it accepts no cursor position, source overlay, source text, stdlib fallback, or `CompletionItem` fields. Import-alias qualifiers fail closed when any file-level body binder could shadow the alias head. No-fact results are stable empty `source.namespace.completion.v1` envelopes. |
| MCP `mw_resource_schema` | `ready` | `marrow_json::resource_schema::resource_schema_for_name` | `name` is required; the tool returns Marrow's resource schema DTO with profile version, catalog-bound resource/store/index/member identities, typed member shapes, and Marrow diagnostic codes. Duplicate same-name resources return `resource.schema.identity` instead of inventing identity from source spelling. All-resource listing remains outside this named lookup surface. |
| MCP `mw_surface_routes` | `ready` | `SurfaceAbiJson::from_program` and `SurfaceRouteManifestJson::from_abi`, filtered by explicit route scope | Returns the Marrow-owned surface route manifest for read operations by default. Explicit `includeWrites` requests return read/update/action routes. It does not open the data store and does not require the MCP data-access opt-in. |
| MCP `mw_surface_read` | `ready` | `SurfaceOperationRequestJson`, `ProjectSurfaceReadSession`, and `execute_project_surface_operation_read_only` | Requires the MCP data-access opt-in and refuses before project or store opening when disabled. Operation bodies are decoded only into Marrow's canonical DTO; read results and canonical surface operation errors are returned as machine-readable envelopes. Write/update/action bodies are rejected by Marrow's read-only executor, not by local semantic classification. |
| MCP `mw_surface_write` | `ready` | `SurfaceOperationRequestJson`, `ProjectSurfaceSession`, and `execute_project_surface_operation` | Requires the MCP data-access opt-in and refuses before project or store opening when disabled. Operation bodies are decoded only into Marrow's canonical DTO; read, update, and action results plus canonical surface operation errors are returned as machine-readable envelopes. Body/tag mismatches are rejected by Marrow's writable executor, not by local semantic classification. |
| MCP `mw_saved_roots` | `presentation-only` | Typed catalog-bound root views with Marrow `data_view_boundary` facts when data access is enabled | Kept as a root listing helper. Production data serving still needs integrity, repair, serve, and attach contracts. |
| MCP `mw_data_children` and `mw_data_read` | `presentation-only` | Stable Marrow saved-data DTOs, catalog-bound typed path segments (`store_catalog_id` for roots and `member_catalog_id` for fields/layers), Marrow-owned JSON Schema helpers for typed saved-data keys and path segments, bounded child pages, Marrow-owned bounded read presence, Marrow-rendered value previews with bounded rendering budgets, and Marrow `data_view_boundary` facts over the real store when data access is enabled; invalid empty paths, zero limits, and old display-string segment authority fail before project or store reads | No local saved-data semantics remain for child expansion, read presence, value preview rendering, input schema construction, or data-view boundary/watch facts. Production data serving still needs integrity, repair, serve, and attach contracts. |
| MCP `mw_data_integrity` | `presentation-only` | Capped current-schema advisory using `marrow_json::saved_data::DataIntegrityResultJson` plus Marrow `data_view_boundary` facts over the real store when data access is enabled | Production validation still needs drift witnesses, typed repair facts, and production validation contracts. |
| MCP saved get/children tools | `deleted` | None | The old untyped path was removed. New saved-data inspection must stay on shared typed DTOs with bounded paging. |
| MCP `mw_run` pure execution/test helper | `ready` | Marrow-owned fresh-memory project session, `ProjectSession::execution_boundary`, canonical entry descriptor invocation, typed run arguments, Marrow run result/error DTOs, Marrow test project session, entry descriptor identity, boundary-carried analysis generation, entry footprint, entry cost shape, entry store-open-mode facts, and locked host | Run mode checks through the normal project path, then uses `ProjectOpen::with_fresh_memory_store()` so the configured project store is not opened. Runtime success and faults return Marrow run envelopes plus `runFacts.executionBoundary` from the opened session, proving a run session over fresh memory with no store stamp; `runFacts.executionBoundary.sourceAnalysisGeneration` is the generation carrier. Test mode opens `ProjectOpen::test()` and returns the Marrow-projected test session `executionBoundary` with `sourceAnalysisGeneration`, `test_memory`, and no store stamp, so discovery and per-test fresh stores stay in Marrow. No durable-data execution claim. |
| DAP initialize and shutdown transport | `ready` | DAP protocol handling and static capability response | Advertised capabilities must stay limited to implemented rows. Conditional breakpoints, exact hit-count breakpoints, logpoints, and stopped-frame hover evaluate are advertised. The DAP `initialized` event is emitted after launch preparation, when Marrow stop-point and debug-expression facts are available for configuration requests. |
| DAP request `arguments` envelope | `ready` | Supported requests validate the top-level `arguments` container before command handlers run: absent or `null` arguments are empty, object arguments are accepted, and concrete non-object arguments fail with `invalid-params`. Unsupported commands stay `unsupported-request`. | No language semantics. |
| DAP launch with configured `defaultEntry` | `presentation-only` | `marrow_run::ProjectSession` opened with isolated writes, canonical entry descriptor invocation, typed argument DTOs, runtime stop-point facts, launch-time `AnalysisSnapshot::generation()`/`AnalysisGeneration` facts bound to the runtime session, Marrow `ExecutionBoundary` DTOs on launch/configuration responses, launch-snapshot source references from `AnalysisSnapshot` source text, runtime debug value facts, stopped-frame durable-data store snapshots, and the Marrow debugger hook | Configured-entry launch does not create accepted catalog state or native stores for absent data dirs, and writes stay isolated. Malformed launch envelope fields fail with `invalid-params`; optional `entry` and `stopOnEntry` `null` values remain absent/default. Existing native stores that would require catalog artifact repair are blocked until Marrow exposes read-only debug catalog admission facts. Local values, local debug expressions, and stopped-frame durable-data reads are Marrow-owned debug facts; the launch response carries preflight Marrow execution-boundary metadata through `marrowDebug.previewExecutionBoundary`, and the configuration response carries the run-thread session boundary through `marrowDebug.executionBoundary`. Stack frames carry bounded per-session source references backed by launch-time analysis source text; durable variable responses carry Marrow data-generation `storeSnapshot` metadata. Production debug launch still needs complete launch/session-wide source-store lifecycle contracts and serve/attach facts before graduating beyond a narrowed debug helper. |
| DAP explicit `entry` launch argument | `presentation-only` | Explicit string selectors are handed to `ProjectOpen::with_entry_override`, then admitted by Marrow's current canonical entry descriptor before launch succeeds; concrete non-string values fail with `invalid-params` | The selector is a transport convenience, not durable identity. Production debug launch still inherits the source/store-generation and serve/attach blockers. |
| DAP non-empty launch `args` | `presentation-only` | Launch args decode to Marrow's typed entry DTOs and are admitted by `CheckedEntryCall::from_protocol_invocation`; non-array values, including `null`, fail with `invalid-params` | The adapter owns only JSON envelope validation. Marrow owns parameter names, catalog IDs, typed values, and admission against the checked program. Production debug launch still inherits the source/store-generation and serve/attach blockers. |
| DAP `setBreakpoints` plain line arming | `presentation-only` | Plain line breakpoints are checked against `CheckedRuntimeProgram::stop_points()` plus runtime file ids/paths. Verified stop-point lines are armed before the run thread starts and can be updated while the run is parked; lines outside Marrow stop points return an `invalid-params` breakpoint contract. Out-of-sequence pre-launch requests remain unverified and unarmed. Malformed source envelopes and non-array breakpoint lists fail with `invalid-params`; positive `sourceReference` values resolve only when they name a per-session source captured from the launch analysis snapshot, otherwise they fail with `invalid-params`. | Serve/attach boundaries and source/store generation are still outside this narrowed debug helper. |
| DAP conditional breakpoints | `presentation-only` | Non-empty `condition` expressions on verified Marrow stop-point lines are checked by `AnalysisSnapshot::checked_debug_expression` at that stop and must type as `bool`; armed conditions are evaluated on the parked run thread with `Frame::evaluate_debug_expression`, including expressions that require durable data. Invalid or non-bool conditions return an `invalid-params` breakpoint contract and are not armed. | Serve/attach boundaries and source/store generation remain blocked. |
| DAP exact hit-count and logpoint breakpoints | `presentation-only` | Exact positive integer `hitCondition` values are DAP hit-count bookkeeping over verified Marrow stop-point lines. `logMessage` templates emit console output and continue without stopping; `{...}` template expressions are checked by `AnalysisSnapshot::checked_debug_expression` at that stop and rendered on the parked run thread with `Frame::evaluate_debug_expression`. Non-string, malformed, or invalid hit/logpoint fields return `invalid-params` contracts and are not armed. | Serve/attach boundaries and source/store generation remain blocked. |
| DAP stop-on-entry and stepping | `presentation-only` | Statement spans and source file paths from the checked runtime hook. Malformed resume `threadId` envelopes fail with `invalid-params`. | Plain line breakpoints and per-session source references now consume Marrow-backed runtime/source facts; production debugger semantics still need store-generation and serve/attach facts. |
| DAP threads, stack trace, scopes, locals, expandable locals, durable data scope | `presentation-only` | Single-thread debug adapter state, checked runtime source paths, launch-snapshot source text for DAP `sourceReference` responses, `Frame::debug_snapshot`/`DebugValue` for bounded local values and expandable local children, and `Frame::debug_data_children`/`Frame::debug_data_preview` for bounded stopped-frame durable data paths plus Marrow data-generation `storeSnapshot` metadata. Malformed stack `threadId`, source `sourceReference`, scope `frameId`, `variablesReference`, variable paging, and variable filter envelopes fail with `invalid-params`; scopes require a current stopped frame; unknown positive variable references remain empty for stale-reference tolerance while unknown source references fail closed. | Local values, stopped-frame source text, and stopped-frame durable data no longer carry blocked runtime metadata, but threads, stack trace, scopes, and launch/session-wide source-store generation remain outside a production debugger contract. |
| DAP watch/REPL/hover evaluate | `presentation-only` | While stopped, `watch`, `repl`, `hover`, and absent-context expressions are checked against the stopped frame file/span by `AnalysisSnapshot::checked_debug_expression`, evaluated on the parked run thread with `Frame::evaluate_debug_expression`, and returned with the same `DebugValue` presentation and expandable-child contract as locals. Expressions that require durable data are admitted by Marrow debug-expression facts and read from the live stopped frame. Invalid expressions fail with `invalid-params`; runtime rejections fail with `runtime-error`; not-stopped requests fail with `invalid-state`. | Serve/attach boundaries and source/store generation remain blocked. |
| DAP durable variables/watch/path | `presentation-only` | The Durable Data scope expands from typed Marrow data path segments through `Frame::debug_data_children`; each child preview is rendered by `Frame::debug_data_preview` with bounded preview limits, and variables responses carry the Marrow data-generation `storeSnapshot` for the stopped-frame read. Stopped-frame watches and conditional breakpoints use `AnalysisSnapshot::checked_debug_expression` and `Frame::evaluate_debug_expression`, not local durable parsing. Malformed evaluate and variables envelopes fail with `invalid-params`; runtime rejections fail with `runtime-error`; unknown stale references remain empty. | Production durable debugger use still needs serve/attach boundaries, launch/session-wide source-store generation facts, and a production paging/generation contract beyond the current stopped-frame debug view. |
| DAP disconnect/terminate/output | `ready` | Adapter lifecycle and captured runtime output. `terminate` succeeds for any live run, including parked runs, staged resumes, and runs that have already resumed; termination suppresses later stale stop events, and `disconnect` remains the shutdown path. | No language semantics. |
| VS Code activation and binary launcher | `presentation-only` | TypeScript starts bundled or configured binaries | Development path resolution is editor-launch plumbing only, not language authority. |
| VS Code TextMate grammar and language configuration | `presentation-only` | Lexical grammar and editor defaults | Grammar is a visual layer only; semantic meaning remains in Marrow/LSP. |
| VS Code Saved Resource Inspector, reveal, and refresh commands | `presentation-only` | Custom LSP saved-root, child-page, bounded value-read, and data-watch-target requests using stable Marrow saved-data DTOs and Marrow data-view boundary facts | Manual refresh remains available. The reveal command lists only current server-returned saved roots and reveals the selected root in the inspector; it accepts no editor-authored saved path. Automatic refresh is limited to LSP-supplied watch targets for the configured native dev-store file and committed catalog lock when `marrow.liveData` is enabled. Root, child-page, and value replies carry Marrow `data_view_boundary`; the view refuses to mix child pages or value reads from different boundaries and does not parse saved paths from source text, guess store paths in TypeScript, reconstruct compatibility from `store_snapshot`, truncate values locally, claim integrity/repair, watch uncommitted writes, watch debuggee state, or attach to served programs. |
| VS Code data integrity command | `presentation-only` | Advisory custom LSP request plus shared Marrow data-view boundary parsing | Parses the returned Marrow `data_view_boundary`, refuses malformed or missing boundary facts with the unavailable contract, and renders the admitted source/store/watch boundary before advisory findings. Debug/admin advisory only, not production validation or repair. |
| VS Code debug contribution and F5 snippets | `presentation-only` | Launch schema for project, explicit entry selectors, typed args, exact string forms for int/date/bytes, stop-on-entry/stepping/plain line breakpoint UI, and DAP-advertised conditional, exact hit-count, logpoint, stopped-frame hover evaluation, and Durable Data scope support | TypeScript only contributes schema/snippets and launches `marrow-dap`; it does not parse or validate Marrow entry values, breakpoint expressions, logpoint templates, or durable data paths. |
| VS Code packaging/bundling | `ready` | Package scripts and bundled binaries | Package gates and audit state must be reported in the package lane. |

## Blocked Marrow Facts

| Blocker | Required fact/API | Surfaces blocked |
| --- | --- | --- |
| Versioned snapshots | Config digest plus catalog, store, and runtime generation carried through LSP snapshots | Workspace, live data, Saved Resource Inspector, MCP data tools |
| Catalog-backed identities | Stable typed IDs for resources, stores, fields, layers, indexes, enum members, aliases, and generated identity constructors | Hover, navigation, rename, semantic tokens, data tools, DAP |
| Split resource/store facts | Separate facts for resource schema, store binding, root, key shape, and store-backed roots | Saved Resource Inspector, saved-root hover, navigation |
| Checker-owned source facts | Remaining checked hover/navigation facts for declarations, module paths, type annotations, docs, enum paths, and saved roots. Catalog-backed definition/reference target facts are exposed through `source_catalog_definition_fact_at` and `source_catalog_reference_facts_at`. Semantic-token role facts for stale-buffer syntax/declarations and snapshot-bound checked binding references, builtins, saved roots, and identity annotations are exposed through `source_semantic_token_facts` and `source_semantic_token_facts_for_file`. | Source tooling |
| Checked saved places and effects | Lowered write plans, durable writes, typed runtime values, transitive effects, and durable scope not covered by current session/data APIs | Data tools, DAP, run/debug UI |
| Debugger run protocol facts | Attach/serve execution boundaries plus launch/session-wide source-store generation facts | DAP launch/evaluate |
| Debugger facts | Production expression evaluation, store-generation facts, and serve/attach boundaries | DAP |
| Store admin facts | Carried generation metadata is not a source/store compatibility verdict. Durable data views still need drift witnesses, typed repair facts, watch/refresh contracts, serve/attach boundaries, and production validation contracts beyond current saved-data read, walk, count, and integrity sampling APIs. | Saved Resource Inspector, MCP data tools, data integrity |
| Typed protocol DTOs | Catalog-epoch-bound DTOs for resources, stores, diagnostics, integrity, repair, debugger values, and admin views | MCP, DAP, VS Code data views |

## Removed Or Blocked Surfaces

The following prior surfaces have no production authority and must not reappear
without a new Marrow fact and a review-gated lane:

- Local durable-data classifiers and private store-key readers.
- Editor or transport options that opt into raw durable-data inspection.
- Launch/run argument paths that construct runtime values without Marrow's typed entry protocol admission.
- Live saved-data hover and live record-count hints.
- TypeScript child-path derivation for the Saved Resource Inspector.
- Local active-call parsing for signature help.
- VS Code launch snippets that seed invalid placeholder entries or raw positional
  args.
- MCP all-resource schema listing without a named resource or paged Marrow DTO.
- Source-spelling identities for saved roots, resource members, indexes, enum
  members, aliases, or physical store keys.

## Handoff Rule

When a lane discovers a missing fact, it records the exact Marrow fact needed and
returns an unavailable contract or deletes the surface. It does not add local
classifiers, compatibility switches, untyped path protocols, message parsing, or
alternate semantic models in `marrow-lsp`.
